<UserControl x:Class="XmlTesterPresentation.FixedStringRuleProps"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:XmlTesterPresentation"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Path: " Margin="5"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Path}" Margin="5" IsReadOnly="True" x:Name="xPath"/>
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Fixed String:" Margin="5"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding FixedString}" Margin="5"/>
    </Grid>
</UserControl>
